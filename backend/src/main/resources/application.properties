# 애플리케이션 이름
spring.application.name=swim-mall

# 데이터베이스 연결 설정
spring.datasource.url=jdbc:postgresql://localhost:5432/swim_mall
spring.datasource.username=postgres
spring.datasource.password=1234
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA 설정
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# 이메일 설정
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=cyjung0416@gmail.com
spring.mail.password=oird ruvw jxzb mfgs
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# 애플리케이션 기본 URL
app.base-url=http://localhost:8080

# 더미 데이터 초기화 (개발 환경에서만 true로 설정)
app.data.init.enabled=true

# 파일 업로드 설정
app.file.upload-dir=uploads
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=25MB

# Toss Payments 시크릿 키 (개발/테스트) - 운영은 환경변수/시크릿 매니저 사용 권장
payments.toss.secret-key=test_gsk_docs_OaPz8L5KdmQXkzRz3y47BMw6

#AI_FEATURE_ENABLED=false